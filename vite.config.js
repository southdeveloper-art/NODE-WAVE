import { defineConfig } from 'vite'

export default defineConfig({
    // Set to '/NODE-WAVE/' for github.io subdirectory
    // Switch to '/' once nodewave.in custom domain is live
    base: '/NODE-WAVE/',
})
